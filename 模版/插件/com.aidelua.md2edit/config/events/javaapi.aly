{
  onCreate=function()
    if searchButton then
      local linearParams=searchButton.getLayoutParams()
      linearParams.setMargins(math.dp2int(8),math.dp2int(8),math.dp2int(16),math.dp2int(16))
      searchButton.setLayoutParams(linearParams)
    end
  end
}